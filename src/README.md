# src/ — 소스 코드 폴더

uv 프로젝트의 src 레이아웃 폴더다. 서비스 패키지 `policy_signal_map/` 하나만 둔다.

- 여기에 다른 파일이나 패키지를 추가하지 않는다
- 패키지 구조와 의존 방향은 [policy_signal_map/README.md](./policy_signal_map/README.md)를 본다
- `tests/`와 `scripts/`는 src 밖(저장소 루트)에 둔다
