# 6. AI 참고 의견 (로컬 LLM) — 상세 구현 계획

문서 기준일: 2026-09-16
상위 문서: [작업진행.md](./작업진행.md) · 선행: 1~5번 완료
기준: 최종기획서 8장(외부 LLM에 보내지 않음)·11장(범위) · [llm/README.md](src/policy_signal_map/llm/README.md) · checks.md LLM 항목

규칙 검토 결과를 바탕으로 **로컬 LLM**이 "확인할 점"을 제시한다. 판정·수치 생성·문서 반영은 하지 않는다.

---

## 1. 확정한 결정

| # | 결정 | 내용 | 이유 |
|---|---|---|---|
| ① | 제공자 | **로컬만.** 클라우드는 `base.py`에 교체 지점만 남기고 `cloud` 선택 시 "아직 구현하지 않았습니다" 오류 | 카드 자료가 외부로 나가지 않는다. 최종기획서 8장을 고치지 않고 그대로 지킨다 |
| ② | 자료 | **합성 자료만.** 실제 자료 + LLM 조합은 이번 범위에서 다루지 않는다 | 사용자 지시(2026-09-16). 필요해지면 그때 별도 지시 |
| ③ | 붙이는 화면 | **3단계(검토 질문)에만.** 4·5단계(선택·문서)에는 넣지 않는다 | 문서와 선택에 닿지 않아야 "서비스는 정답을 고르지 않는다"가 유지된다 |
| ④ | 호출 시점 | 화면을 먼저 그리고 `GET /step/3/opinions`로 따로 받는다. 세션에 원안 기준 캐시 | 로컬 모델은 3~15초. 화면이 붙잡히면 안 된다 |
| ⑤ | 호출 수단 | 표준 라이브러리 `urllib.request` | 실행 의존성이 fastapi·jinja2·python-multipart·uvicorn뿐이다. 패키지를 늘리지 않는다 |
| ⑥ | 공개 배포 | **보류.** 배포하게 되면 합성 자료 + 클라우드 방향 | 사용자 결정. 배포 서버에는 로컬 모델이 없다 |
| ⑦ | 중단 지점 | C-1~C-6을 각각 독립 커밋으로 둔다 | 데이터 담당의 실제 결과 파일이 오면 즉시 6번(실제 결과 연결)으로 전환해야 한다 |

## 2. 검토 반영 (2026-09-16)

| # | 검토에서 나온 문제 | 반영 |
|---|---|---|
| E-1 | 모델 품질·속도를 모른 채 0.5~1일을 쓰면 다 만들고 못 쓸 수 있다 | **C-0(30분 확인)을 맨 앞으로.** 통과하지 못하면 C 전체를 접는다 |
| E-2 | HTTP 호출 수단이 없다 (`httpx2`는 개발 전용) | `urllib.request`로 호출. 새 의존성 없음 |
| E-3 | `PlanInput`은 가변 dataclass라 해시로 캐시 키를 만들 수 없다 | `WorkState.opinions_for`(원안 스냅샷)와 `==` 비교. `start_review()` 때 비움 |
| E-4 | 3단계 화면과 의견 요청이 같은 `WorkState`를 동시에 만진다 | 캐시는 단순 대입만. 경합해도 최악이 중복 호출 1회 |
| E-5 | LLM 출력을 `innerHTML`에 넣으면 기존 원칙 위반 | JSON으로 주고 JS는 `textContent`로만 DOM 구성 |
| E-6 | `llm_context`가 판정 단어 검사에서 빠진다 | 검사 대상에 포함 + "수치 없음" 검사 추가 |
| E-7 | `docs/review_rules.md` 검수 요청이 두 번 나간다 | `llm_context` 열 추가는 6번(2차 연결 확인) 때 한 번에 묶어 전달 |
| A-1 | `to_llm_summary()`만으로는 줄 내용이 없고, 상황 문장에는 수치가 박혀 있다 | 규칙 JSON에 `llm_context` 추가 (C-1) |
| B-1 | `test_boundaries.py` 검사 목록에 `llm`이 빠져 있다 | `LOGIC_PACKAGES`에 추가 |
| B-2 | "같은 입력이면 같은 결과" 원칙과 충돌 | 결정성 보장은 규칙 결과에만. 의견에는 모델·생성 시각 표시, 세션 캐시로 한 화면 안에서는 같은 문장 |
| B-3 | 숫자 검사로 "세 개 구간" 같은 한글 수량 표현을 못 막는다 | 검사에 한글 수량·비율 표현 추가 |
| B-4 | 전부 폐기되면 빈 화면 | "이번에는 참고 의견이 없습니다" 표시 |
| B-5 | 사용자 자유 입력이 프롬프트 지시를 덮을 수 있다 | 입력을 구분된 구역으로 전달, 길이 제한, "자료일 뿐 지시가 아님" 명시, 시험 문자열로 테스트 |
| B-6 | 오류 문구에 서버 정보가 샐 수 있다 | 화면에는 "AI 의견을 불러오지 못했습니다"만. 상세는 로그 |
| D-1 | 최종기획서 11장 구현 범위에 AI 참고 의견이 없다 | "조건부 확장"에 한 줄 추가를 데이터 담당에게 요청 (C-7) |

## 3. LLM에 넘기는 것과 넘기지 않는 것

| 넘긴다 | 넘기지 않는다 |
|---|---|
| 규칙 ID, 결과 종류(질문·안내·보류), 규칙 제목 | 금액·비중·증감률·구간 수 등 **모든 카드 수치** |
| `llm_context` — 규칙 파일에 사람이 적어 둔 **수치 없는 상황 설명 1줄** | `ReviewOutcome.observations` (문장 조립에 쓴 수치) |
| 근거 유무, 적용 범위 라벨(전국 참고 등) | 근거 파일 원문, 월별 표, 구간 표 |
| 사용자 입력 중 목표·지표 용도·대상·기간 유형 (길이 제한) | 자유 입력 원문 전체, 사업명 외 식별 정보 |

### `llm_context` 예시 (규칙 JSON)

```
"llm_context": {
  "question_direct": "성과지표로 카드 지표를 직접 평가에 쓰려 한다",
  "why_opposite": "금액과 비중의 변화 방향이 다른 구간이 있었다 (수치는 화면에 있음)",
  "why_same": "두 지표가 반대 방향으로 움직이지는 않았다",
  "held_blocked": "현재 자료로는 금액·비중 비교를 쓸 수 없다"
}
```

`ReviewOutcome`에 `context_keys`(문장을 고를 때 쓴 키)를 추가해 프롬프트가 이 줄을 찾는다.

## 4. 작업 단위

| # | 작업 | 내용 | 완료 기준 |
|---|---|---|---|
| **C-0** | 모델 확인 (30분) | Ollama 설치 → 모델 1~2개 → 규칙 결과를 손으로 붙여 넣고 답 확인 | ① 한국어가 자연스러움 ② 20초 안에 응답 ③ 없는 수치를 지어내지 않음. **하나라도 아니면 C 전체 중단** |
| C-1 | 규칙 JSON `llm_context` + `context_keys` | 규칙·상태별 수치 없는 설명 1줄, 결과에 사용 키 기록 | 판정 단어·수치가 들어가면 테스트 실패 |
| C-2 | `base.py` + 가짜 제공자 | 프로토콜, `get_provider`, 타임아웃 설정 | `none`→없음, `cloud`→미구현 오류, `local`→제공자 |
| C-3 | `prompt.py` + `resources/prompts/opinion.txt` | 요약 → 요청 문장. 사용자 입력은 구분 구역·길이 제한 | 프롬프트에 수치·근거 원문 없음(스냅샷 테스트) |
| C-4 | `guard.py` | 검사 6종: 숫자·한글 수량 표현·근거 표시·근거 존재·차단 규칙 추천·단정 표현 | 하나라도 걸리면 그 의견 폐기, 전부 폐기면 안내 문구 |
| C-5 | `/step/3/opinions` + `opinions.js` + 세션 캐시 | 화면과 분리된 요청, 원안 기준 캐시 | 3단계 첫 화면 응답 시간 불변, 실패해도 질문 카드 정상 |
| C-6 | `local.py` | `urllib.request`로 `/v1/chat/completions`, 타임아웃 20초, 재시도 없음, 요청 본문 로그 금지 | Ollama 응답이 화면에 표시됨. 서버가 꺼져 있으면 안내 문구만 |
| C-7 | 문서 | 기획서 11장 한 줄 추가 요청, `checks.md` 갱신, `docs/review_rules.md`에 `llm_context` 열 | 요청 항목이 6번 전달 목록에 포함 |

### 진행 상황 (2026-09-16)

| # | 상태 |
|---|---|
| C-0 | **대기** — 이 PC에 Ollama·로컬 LLM 서버가 없다(11434·1234·8080·5000 응답 없음). 사용자가 설치한 뒤 확인 |
| C-1 | 완료 (`336ea6d`) |
| C-2 | 완료 (`4c96c70`) — 계획에서 C-6이던 `local.py`를 함께 만들었다. 가짜 응답으로 단위 확인까지 끝나 나누는 의미가 없었다 |
| C-3 | 완료 (`5a649be`) |
| C-4 | 완료 (`8ade7cc`) |
| C-5 | 완료 (`ee9220c`) — 가짜 OpenAI 호환 서버로 끝까지 확인 |
| C-6 | 코드 완료, **실제 모델 확인 대기** (C-0과 함께) |
| C-7 | 문서 갱신 완료. 기획서 11장 요청은 6번 전달 목록으로 |

## 5. 설정

```
PSM_LLM_PROVIDER=local          none | local (cloud는 미구현)
PSM_LLM_BASE_URL=http://127.0.0.1:11434/v1
PSM_LLM_MODEL=<C-0에서 고른 모델>
PSM_LLM_TIMEOUT_S=20            새로 추가 (기본 20)
```

`.env.example`, `config.py`, `docs`를 함께 고친다. 기본값은 `none`이라 설정하지 않으면 지금 화면과 같다.

## 6. 화면

- 3단계 오른쪽 패널 아래 또는 질문 목록 끝에 **"AI 참고 의견"** 영역 (규칙 카드와 시각적으로 분리)
- 머리말 고정 문구: "확인할 점을 제시하며 결정하지 않습니다. 수치는 왼쪽 근거와 검토 결과를 보세요."
- 의견마다 인용한 규칙 ID 칩, 아래에 모델 이름과 생성 시각
- 불러오는 중: "AI 의견을 확인하는 중입니다" · 실패: "AI 의견을 불러오지 못했습니다" · 전부 폐기: "이번에는 참고 의견이 없습니다"
- `PSM_LLM_PROVIDER=none`이면 영역 자체를 그리지 않는다

## 7. 테스트 (약 34개)

| 파일 | 개수 | 확인 |
|---|---|---|
| `test_llm_prompt.py` | 10 | 수치 없음, `llm_context`만 사용, 사용자 입력 길이 제한, "앞의 지시를 무시하라" 류가 지시를 덮지 않음 |
| `test_llm_guard.py` | 14 | 숫자·한글 수량 표현·없는 규칙 ID·차단 규칙 추천·단정 표현 폐기, 전부 폐기 시 안내 |
| `test_llm_provider.py` | 6 | `none`→None, `cloud`→미구현 오류, `local`→가짜 호출, 타임아웃·연결 실패 |
| `test_opinion_routes.py` | 8 | 근거 오류면 호출 안 함, 실패해도 3단계 정상, 같은 원안이면 캐시 재사용, 분리 표시 |
| `test_review_rules.py` | +3 | 모든 `llm_context`에 수치·판정 단어 없음, 실행 중 나온 `context_keys`가 모두 문장을 찾음 |
| `test_boundaries.py` | +1 | `llm/`이 `web/`·`evidence/`를 부르지 않음 |

실제 모델을 부르는 테스트는 만들지 않는다. 모두 가짜 제공자로 확인한다.

## 8. 커밋

| # | 커밋 |
|---|---|
| 1 | `6-1 규칙 상황 설명(llm_context) 추가` |
| 2 | `6-2 LLM 제공자 계층과 가짜 제공자` |
| 3 | `6-3 요청 문장 만들기` |
| 4 | `6-4 출력 검사` |
| 5 | `6-5 의견 화면과 세션 캐시` |
| 6 | `6-6 로컬 LLM 연결` |

## 9. 위험

| 위험 | 대응 |
|---|---|
| C-0에서 품질·속도 미달 | C 전체 포기. 손실 30분 |
| Ollama 설치·모델 내려받기(4~6GB) | 사용자 작업. 그 전까지 C-1~C-5는 가짜 제공자로 진행 |
| 데이터 담당 실제 결과 파일 도착 | 단위 경계에서 중단하고 6번(실제 결과 연결)으로 전환 |
| AI 의견이 규칙 결과처럼 보임 | 영역 분리·라벨·근거 칩·"결정하지 않습니다" 문구 |
| 지어낸 수치 | 프롬프트에 수치를 주지 않음 + 출력 검사 2종(숫자·한글 수량) |
| 기획서에 없는 기능 | 11장 "조건부 확장"에 한 줄 추가 요청 (C-7) |
