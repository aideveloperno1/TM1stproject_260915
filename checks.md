# checks.md — 합의·연결 확인·테스트·남은 작업

기준 문서: [최종기획서](../최종기획서.md) · [개발업무 워크플로우](../개발업무_워크플로우.md)

## 1. 운영 합의

| 항목 | 결정 내용 | 상태 |
|---|---|---|
| 데이터 분석 담당 | (이름) | 미정 |
| 서비스 개발 담당 | (이름) | 미정 |
| 【들어오는 파일】 분석 근거 파일 전달 방법 | **방법을 미리 정하지 않는다(9/18).** 직접 전달(USB 등)이나 두 사람만 접근하는 비공개 공유 폴더 중 편한 방법을 쓰되, 단체방·메신저·이메일·GitHub·외부 AI 도구로는 보내지 않는다(보안 서약). 받은 파일은 `private/`에 둔다 | 결정(사용자, 9/18) |
| 【들어오는 파일】 분석 근거 파일명 | `review_evidence_real_v{버전}.json` (합성: `review_evidence_demo_v{버전}.json`) | 제안 |
| 【들어오는 파일】 서비스에서 불러오기 | 합성/실제 파일 연결 방식과 LLM 선택과의 조합은 개발 진행 상황을 보고 개발 담당이 결정. 우선 합성 파일을 코드에 연결해 개발 | 보류(개발 담당 결정 예정) |
| 코드 폴더 | 작업 폴더 아래 `Service` (uv 프로젝트, 로컬 git). `checks.md`도 이 폴더에서 관리. 합의 당시 경로는 `1차공모\Service`였고 9/18 현재 상위 폴더 이름만 바뀌었다 | 결정(개발) |
| 폴더 구조 | 기능별 패키지(plan·evidence·review·choices·document·llm)와 웹 계층(web) 분리, 코드가 읽는 자료는 `resources/`. 의존 방향 web → document → choices → review → evidence·plan, llm은 review 결과만 사용. 상세는 README | 결정(개발) |
| 데이터 담당 산출물 위치 | 저장소 밖 상위 폴더 (data_notes.md, 분석 코드). 서비스와는 `review_evidence.json`만 주고받음. 합의 당시 이름은 `1차공모\Analysis\`였고, 9/18 현재는 상위 폴더의 `Data\`·`Data2\`로 받고 있다 | 결정(개발), 데이터 담당 공유 필요 |
| 개발 환경 | uv + Python 3.12 (`.python-version`), 의존성은 `pyproject.toml`·`uv.lock` | 결정(개발) |
| 코드 저장소 | GitHub **public** 저장소 https://github.com/aideveloperno1/TM1stproject_260915 (9/15 연결·공개 전환). 실제 분석 근거 파일(`private/`, `*_real_*.json`)과 `.env`(API 키)는 `.gitignore`로 제외 | 결정(개발) |
| 라이선스 | 오픈소스 라이선스 없음(모든 권리 보유). 9/15 GPL v3 LICENSE 삭제, README에 권리 고지. 이유: 참가신청서류 준수사항 5항(수상작 지식재산권 비씨카드 귀속)과 GPL의 영구 이용 허락이 충돌. 이력의 초기 커밋(`2cb4e19`)에는 LICENSE가 남아 있음 | 결정(개발), 데이터 담당 공유 필요 |
| 지역 선택 목록 출처 | `데이터/2` 주민등록인구 CSV(공개 자료)의 시도·시군구 이름. 카드 CSV는 사용하지 않음 | 결정(개발) |
| 화면 스타일 | 직접 작성한 CSS. 디자인은 `기획서 보완 서비스 랜딩페이지` 폴더(정책맵 랜딩·워크스페이스 시안) 참고 | 결정(개발) |
| CSS 파일 나누기 | **나누지 않음.** `style.css` 한 파일로 유지(9/17 약 1,200줄). 나눠도 동작·속도는 같고, 규칙 순서가 바뀌면 인쇄 모양 등이 달라질 위험만 생김. 처음 README에 적었던 "700줄 넘으면 분리" 기준은 폐기 | 결정(사용자, 9/17) |
| 기술 방식 | 전부 Python: FastAPI + Jinja2 템플릿(서버 렌더링) + 화면 반응용 소량 JS. 테스트는 pytest. (9/15 Vite + TypeScript에서 변경) | 결정(개발) |
| 작업 상태 보관 | 서버 메모리 + 세션 쿠키. 서버 재시작 시 초기화, 로그인·장기 보관 없음 | 결정(개발) |
| 차트 | Chart.js (CDN으로 템플릿에서 사용) | 결정(개발) |
| 공개 배포 호스팅 | 서버 메모리 세션을 쓰므로 요청마다 인스턴스가 바뀌는 서버리스(Vercel 함수)에서는 작업 상태가 사라질 수 있음. 상시 실행 서버(Render, Railway, Fly.io 등) 또는 상태를 브라우저에 두는 방식 중 결정 필요 | 보류 (공개 배포 자체가 보류, 9/16) |
| 화면 흐름 | 시안의 5단계 (기획 입력 → 근거 확인 → 검토 질문 → 보완 선택 → 보완 기획안). 기획서의 "실행 조건"은 보완 선택 화면의 추가 입력 칸, "출력"은 보완 기획안 화면의 [결과 저장] | 결정(개발) |
| 사업 목표 선택지 | 최종기획 기준 (금액 확대/비중 확대/참여 상점 이용 확대/기타, 여러 개) | 결정(개발) |
| 지표 용도 | 직접 성과평가 / 참고 현황 / 아직 모름 ("아직 모름"은 결론 없이 선택지만 제시) | 결정(개발) |
| 보완 선택지 | 시안의 A~D 대안 + 원안 유지·보류 + 수정(선택한 대안 내용 편집) | 결정(개발) |
| 보완 기획안 화면 버튼 | [결과 저장], [전체 문서 보기], [정밀 분석 요청서 초안], **[PDF로 저장]** | 결정(개발) · 9/16 변경: 인쇄/PDF 제외 → 브라우저 인쇄 방식으로 추가 (`.md`는 고쳐 쓰는 원고, PDF는 공유용 사본. 서버 PDF 생성기는 한글 글꼴·배포 부담으로 쓰지 않음) |
| 검토 규칙 표시 | 최종기획 기준: R07 검토 구현, R01·R03·R04·R05 기본 검토, R06 분석 예시, R02 향후 기능 | 결정(개발) |
| 시도(제주) 근거 추가 | **이번 제출 범위에서 제외, 향후 확장.** 근거 파일은 전국 레코드만 사용한다. 입력 지역과 근거 `region_key`를 잇는 규칙, 지역별 안내 문구, 예시 기획 지역 변경, 캡처·사용법 재작성이 함께 필요해 제출 일정(9/21)과 겹친다. 제출 PDF에는 "조건을 갖춘 시도로 확장 가능"으로만 소개 | 결정(사용자, 9/18) |
| 업종 비율 화면 계산 기능 | **이번 제출 범위에서 제외, 향후 확장.** 근거 파일 형식이 업종 합계 기준이라 형식 재합의·검증·시험 자료가 모두 바뀌고, 화면에 업종 비율을 띄우면 추천으로 읽혀 "판정하지 않는다" 원칙과 부딪힌다. R06은 분석 예시, R02는 향후 기능 표시 유지 | 결정(사용자, 9/18) |
| 쿠폰 발급·사용·정산 실적 합성 | **만들지 않음.** R07 대안 A는 수집 계획을 기획안에 적는 기능이라 실적 수치 없이 끝까지 동작한다. 시행 전 사업의 실적 수치는 합성이라고 밝혀도 효과가 있었던 것처럼 읽힐 수 있다(최종기획서 10장) | 결정(사용자, 9/18) |
| 검토 규칙 관리 번호(R01~R07) 화면 표시 | **화면과 보완 기획안에서 완전히 뺀다.** 질문·선택 카드, 1단계 예정 목록, 3단계 오른쪽 목록, 함께 확인 안내, AI 의견 근거 배지, 5단계 변경 표·근거 추적, 문서 별첨 표까지 모두 **규칙 제목**을 쓴다. 번호는 `resources/rules/review_rules.json`·`docs/review_rules.md`·데이터 담당 검수 등 **내부 문서에만** 남긴다 | 결정(사용자, 9/18) |
| 랜딩 페이지 | 워크스페이스 우선. 랜딩은 9/19~20 여유 시 추가 | 결정(개발) |
| 시안 오류 수정 | 자료 버전은 불러온 파일에서 표시(합성은 `demo-`), 방향 판정은 반올림 비중이 아닌 원 금액 교차곱(워크플로우 8-2) | 결정(개발) |
| 규칙 원본 | `resources/rules/review_rules.json`이 원본, `docs/review_rules.md`는 사람이 읽는 설명 | 결정(개발) |
| 【나가는 파일】 보완 기획안 저장 | [결과 저장] 클릭 → 저장 위치 선택 창 → 사용자가 고른 폴더에 Markdown 저장. 선택 창을 지원하지 않는 브라우저는 다운로드 폴더로 저장하고 안내 | 결정(개발) |
| 【나가는 파일】 기본 파일명·별첨 | `보완기획안_{사업명}_{날짜}.md`, 별첨(변경 전후와 근거)은 같은 파일 맨 뒤 | 결정(개발) |
| 입력폼 필수·선택 | 필수: 사업명, 목표, 대상, 지역, 기간, 현재 성과지표, 지표 용도 / 선택: 예산, 사용처, 자료 확보 상태, 변경 불가 조건 (비우면 기획안에 "추가 확정 필요") | 결정(개발) |
| 입력폼 목표 | 여러 개 선택 가능 (금액 확대/비중 확대/참여 상점 이용 확대/기타) | 결정(개발) |
| 입력폼 지역 | 전국/시도/시군구 선택 목록 | 결정(개발) |
| 입력폼 기간·예산 | 기간은 시작일~종료일, 예산은 금액 칸 + [미정] 체크 (미정과 0원 구분) | 결정(개발) |
| 공개 배포 | 공개 배포본에는 합성 자료만 포함. 실제 분석 근거 파일은 git 제외 폴더(`private/`)에만 둠 | 기획 확정 |
| 구현 순서 | A(규칙 기반 R07 사례 완성) 후 C(LLM 추가 점검 의견) 도입 | **9/16 구현 완료(로컬 전용).** 외부 전송이 없어 기획서 8장 변경은 불필요. 11장 "조건부 확장"에 한 줄 추가할지는 미정(6장) |
| LLM 제공자 | **로컬만 사용.** 클라우드는 설정·차단 규칙과 교체 지점만 남기고 호출 코드는 만들지 않음 | 결정(사용자, 9/16). 필요해지면 합성 자료 + 클라우드 방향 |
| 공개 배포 | 보류 | 결정(사용자, 9/16). 배포하면 로컬 LLM은 동작하지 않으므로 AI 영역 표시 방법을 그때 정함 |
| 실제 자료 + 로컬 LLM | **허용.** 로컬 모델은 PC 밖으로 보내지 않고, 모델에는 수치 없이 규칙 결과와 상황 설명(예: "금액과 비중의 변화 방향이 서로 달랐던 인접 월 구간이 있었다")·담당자 입력만 넘긴다. 실제 자료 + 클라우드는 계속 차단. 2차 연결 확인(6-1) 숫자 대조 중에는 AI 의견을 끈다(`PSM_LLM_PROVIDER=none`) | 결정(사용자, 9/17) |
| AI 모델 선택 | **담당자가 3단계에서 AI 참고 의견 모델을 고르는 정식 기능.** 고를 수 있는 모델은 `.env`의 `PSM_LLM_MODELS`, 선택 위치는 3단계 AI 영역, 받아 두지 않은 모델은 고를 수 없게 표시, 모델별 의견을 나란히 비교하지 않음. 의견은 여전히 선택·문서에 반영하지 않음 | 결정(사용자, 9/17) · 구현 C-8 |
| AI 응답 시간 한도 | 120초 (C-0: 모델 전환 후 첫 응답 약 40초, PC를 켠 뒤 Gemma 첫 호출 약 74초). 처음 60초로 정했다가 같은 날 120초로 변경. `.env`의 `PSM_LLM_TIMEOUT_S`로 바꿈 | 결정(사용자, 9/17) |
| LLM 연결 구조 | 제공자를 설정으로 고르는 공통 연결 계층(`llm/base.py`). 로컬만 구현했고 클라우드는 `cloud.py`를 추가하면 붙는 자리만 있음. API 키를 쓰게 되면 서버 환경변수에 둠 | 결정(개발), 9/16 로컬만 구현 |
| LLM 전달 자료 | ① 공개 시연은 합성 자료만 사용 ② LLM에는 수치 없이 규칙 결과(규칙 번호·종류·제목·적용 범위)와 규칙별 수치 없는 상황 설명(`llm_context`), 사용자 목표·대상·지표·지표 용도만 전달. 수치는 규칙 엔진이 화면에 표시 | 개발 담당 동의, 9/16 구현. 데이터 담당 합의 전 |
| LLM 출력 원칙 | 수치·수량 표현 금지, 규칙 번호 표시와 존재 확인, 검토하지 않은 규칙 제안 금지, 판정 표현 금지, "AI 참고 의견"으로 분리 표시, 기획안·선택에 반영하지 않음 | 9/16 구현(`llm/guard.py`). 데이터 담당 합의 전 |

## 2. 연결 확인 일정

| 차수 | 일시 | 확인 내용 | 결과 |
|---|---|---|---|
| 1차 | 9/17 | 첫 결과 1건을 서비스가 오류 없이 읽는가 (필드명·단위·상태값) | **사전 점검 — 확정본 도착 후 다시 확인.** 데이터 담당 임시본(9/16, 확정본 아님)으로 값은 보지 않고 구조·계산 일치 여부만 확인했다. **읽기:** `data_kind` 값과 `month` 표기 두 곳이 9-3 형식과 달라 서비스가 파일을 읽지 않음 → 확정본에 반영 요청. 두 곳만 고친 사본(메모리 안)으로는 규칙 실행과 근거 화면 데이터 생성까지 진행됨. **계산:** 파일에 담긴 인접 월 비교 결과가 서비스 계산과 모두 일치.**추가 칸:** `comparisons`(경고 후 무시). 유지 여부는 데이터 담당과 결정 |
| 2차 | 9/18 (시간) | 1~6월 실제 결과의 수치·질문·문서 반영이 분석 결과와 같은가 | |
| 3차 | 9/20 (시간) | PDF에 넣을 화면·문서 최종 대조 | |

## 3. 전달 형식

- `review_evidence.json` schema 2.0 (워크플로우 9-3장)
- 금액: 원 단위 정수 / 비중: 퍼센트 값 (8% → `8.0`)
- 구조·단위 변경 시 데이터 담당이 버전을 올리고 알림. 개발 담당은 결과 파일을 직접 수정하지 않음

상태: **데이터 담당 확정 (9/15 결정 문서, 저장소 밖)** — 9-3 예시 형식 그대로, 금액 원 단위 정수, 비중 퍼센트 값(미리 반올림하지 않음), `calculation_status`·`applicability`·`reason` 필수, 행이 없거나 분모가 0 이하면 0으로 채우지 않고 상태값으로 표시. 원본 CSV는 모든 열을 문자열로 읽고 금액·건수만 숫자로 변환. 9/17 임시본 사전 점검에서 `data_kind`·`month` 표기 두 곳이 달라 확정본에서 다시 확인한다(2장 1차).

## 4. 테스트 (워크플로우 12장)

| 시험 | 기대 결과 | 결과 |
|---|---|---|
| GENDER_CD=x와 AGE_CD=x가 섞임 | U는 성별 컬럼 미상만 집계 | 데이터 담당 소관 (원본 집계) · **정의 확인 9/15 (데이터 담당 결정 문서):** U는 `GENDER_CD` 정규화 값 `x`로만 정하고 `AGE_CD=x`라는 이유만으로 넣지 않음, 허용 값 `1`·`2`·`3`·`x`이고 그 밖은 `invalid_input`. 이 정의로 1~6월을 다시 계산한 결과가 최종기획서 4-2 표와 일치(수치는 기획서에만). 원본 집계 단계라 서비스 테스트 대상은 아님 |
| 숫자·코드 오류 | invalid_input과 사유 표시 | 통과 9/16: 계산(`test_evidence_loader`) · 화면은 오류 화면 503에 수치 없는 문구 (`test_step_two_error_page_when_evidence_fails`) |
| 분모 0·자료 없음 | null과 정확한 상태, 나누기 오류 없음 | 통과 9/16: 계산 + 화면 "계산 불가"·"분모 0 · 계산 불가"·보류 구간 (`test_step_two_expected_values`) |
| 미상 0 또는 관측 행 없음 | 값과 관측 상태를 구분, 실제 0 단정 없음 | 통과 9/16: no_data에 0 거부, 화면 "자료 없음", 0이 아닌 작은 값은 "0.01 미만" (`test_small_nonzero_values_are_not_shown_as_zero`) |
| 비교 A / 비교 B | 별도 결과로 계산·표시 | 통과 9/16: 계산 + 구간 표의 별도 열 |
| 이전 F=0·변화 없음·월 누락 | 증감률 null·반대 아님·비교 보류 | 통과 9/16 (`test_evidence_compare`, 화면 "계산 불가"·"보류 — 사유") |
| 아주 작은 방향 차이 | 중립적 표시, 강한 경고·자동 수정 없음 | 통과 9/16: 화면 "0.01%p 미만 (증가)", 경고 클래스 없음 (`test_step_two_shows_tiny_change_label`, `test_step_two_has_no_red_warning_classes`) |
| 방향이 같은 두 기간 | 반대 방향 예시 문구를 복사하지 않음 | 2단계 요약 문장 통과 9/16 (`test_same_direction_sentence_has_no_opposite_wording`) · 3단계 "왜 묻나요" 문구 통과 9/17 (`test_same_direction_does_not_reuse_opposite_wording`) |
| 전국 자료 + 특정 지역 기획 | 전국 참고 표시 | 2단계 통과 9/16 (범위 칩·지역 안내 문구, `test_region_note_for_sigungu_plan`) · 3단계 통과 9/17 (질문 카드 범위·지역 안내가 2단계와 같음, `test_region_note_and_scope_label_match_evidence_screen`) · 5단계 통과 9/17 (근거 추적에 "전국 참고 — 특정 지역의 진단이 아님", `test_changed_line_carries_evidence_chip_and_trace`) |
| 검증 시도 자료 + 시군구 기획 | 넓은 범위 참고 표시, R02 자동 활성화 없음 | R02는 항상 "검토하지 않음"으로 표시 9/16 (`test_r06_and_r02_are_not_reviewed`) · 시도 자료 연결은 C-2 결정 후 |
| 참고용 지표 | 불필요한 직접 성과 오류 없음 | 통과 9/16: 지표 용도가 참고 현황이면 안내만 표시하고 질문 0건, 판정 단어 없음 (`test_reference_indicator_use_is_notice_without_error_wording`) |
| 자료 수집 미정 | 추가 확정 필요 표시 | 통과 9/16: 빈 담당자·주기가 문서에 `[추가 확정 필요]`로 남고 8장에 모임 (`test_empty_owner_and_cycle_become_pending_marks`, `test_plan_pending_items_are_listed_once`) · 9/17 6-5b: 쓰지 않게 된 실행 조건은 8장에 남지 않음 — 질문이 사라짐·원안 유지로 변경·대안 A→B (`test_disappeared_question_leaves_no_execution_pending`, `test_keep_original_after_adopt_leaves_no_execution_pending`, `test_switching_a_to_b_leaves_no_execution_pending`), 문장에서 쓰는 주기는 유지 (`test_r04_b_keeps_entered_cycle_after_r07_is_kept_original`) |
| 목표 유지·대안 취소 | 미채택 내용이 최종 문서에 남지 않음 | 통과 9/16 (`test_cancelled_choice_leaves_no_trace`, `test_without_choices_document_keeps_original_and_has_no_changes`) |
| 목표·지역 변경 | 관련 선택과 문서 재검토 | 통과 9/16: 재확인 필요가 남으면 문서를 만들지 않고 4단계로 되돌림 (`test_unanswered_question_sends_back_to_step_four`, `test_changed_plan_marks_choice_for_recheck`) · 9/17 6-5a: 다시 저장하면 재확인이 풀리고 5단계로 감, 4단계를 건너뛰어도 5단계·내려받기가 재확인을 요구 (`test_saving_again_clears_recheck_and_opens_step_five`, `test_step_five_without_step_four_still_asks_for_recheck`, `test_download_without_step_four_is_blocked_after_plan_change`), 보관 안내는 이번 변경분만 (`test_archive_notice_shows_only_the_latest_plan_change`) · 브라우저 확인 9/17 (5장) |
| Markdown 저장 | 저장 위치 선택 후 외부에서 열었을 때 화면과 내용 일치 | 자동 확인 9/16: 내려받기 본문이 화면 내용과 같고 한글 파일명 인코딩 정상 (`test_download_sends_markdown_with_korean_filename`) · 9/17 6-5d: 5단계를 연 뒤 원안이 바뀌면 [결과 저장]이 재확인을 안내하고 내려받지 않음 (`test_save_script_does_not_download_recheck_response`, 브라우저 확인) · **외부 편집기로 열어 보기·실제 저장 위치 선택 창은 사용자 확인 대기** |
| PDF 저장 | 인쇄 결과에 화면 전용 요소가 없고 표·변경 표시·고지가 남음 | 사용자 검토 완료 9/16 · 개발 확인: 인쇄 규칙 14개 적용, A4 폭 186mm에서 표 잘림 없음(654px), 변경 줄은 배경색 없이 왼쪽 선으로 구분 (`test_pdf_button_is_on_every_result_screen`, `test_print_stylesheet_hides_screen_only_parts`) |
| 공개 배포 | 실제 제한 자료 없이 합성 표시 유지 | 저장소 검사 통과 9/16 (`check_public_bundle.py` 위반 0) · 배포본 확인은 7단계 |

## 5. 진행 기록

| 날짜 | 완료한 일 | 확인 방법 |
|---|---|---|
| 9/15 | 기획 입력 화면(S01) TypeScript 버전 구현 (커밋 `37071ee`) | `npm run build`, 브라우저 확인 |
| 9/15 | 개발 환경을 uv + Python 3.12로 전환하고 기획 입력 화면(S01)을 FastAPI + Jinja2로 다시 구현: 필수·선택 항목, 목표·지표 복수 선택, 전국/시도/시군구 선택(시도 17·시군구 269), 예산 미입력/미정/금액 구분, 서버 검증·오류 표시, 추가 확정 필요 요약, [검토 시작] 시 원안 보관, 원안 변경 후 재제출 시 재확인 표시, 예시 기획 채우기 | `uv run pytest` 15개 통과, 브라우저에서 예시 채우기→시도 변경 시 시군구 목록 갱신→검토 시작→2단계 원안 표시, 콘솔 오류 없음 |
| 9/15 | 폴더 구조 전환: plan/·review/·web/(routes·templates·static)·resources/ 분리, 규칙 표시를 `review_rules.json`에서 읽도록 변경, 테스트를 검증·화면 흐름으로 분리, `../Analysis/` 폴더와 안내 README 생성 | `uv run pytest` 18개 통과, 지역 목록 재생성 결과 변경 없음, 실행 중 서버에서 입력 화면·규칙 목록·정적 파일 응답 확인 |
| 9/16 | 서비스 완성 기준 폴더 전체 생성(evidence·choices·document·llm·web 하위·resources 하위·tests/fixtures·docs·private 등 26개), 폴더마다 README.md에 만들 파일·상세 사양·원칙·테스트·일정 작성. `private/`는 README만 추적되도록 `.gitignore` 조정 | 모든 폴더 README 존재 확인, `git add --dry-run`에서 private는 README만, `uv run pytest` 18개 통과 |
| 9/16 | 1. 근거 계산 계층 완료 (계획 검토 11건 반영): 설정(`config.py`, 실제 자료+클라우드 LLM 차단), 근거 파일 형태·검증(수치 비노출 오류 문구, 규칙 선행 조건)·로더, 경계 사례 22개(생성 스크립트), 비교 A/B(정수 교차곱)·기간 요약, 합성 근거 파일 demo-001, 공개 자료 검사(한글 경로·원자료 csv 포함)·의존 방향 검사 | `uv run pytest` 119개 통과, `check_public_bundle.py` 추적 파일 106개 위반 0, 금지 import 임시 삽입 시 경계 테스트 실패 후 되돌려 통과 확인. 소수점 판정이 틀리는 큰 금액 사례에서 교차곱 정답 확인 |
| 9/16 | 4. 보완 선택 완료: 기본 검토 규칙에 대안 추가(R03·R04는 R07과 실행 조건 공유), 안내는 선택 가능·필수 아님, 보류·추가 확정 필요는 선택 없이 문서 기록, 원안 변경 비교(`plan/changes.py`), 선택 로직(검증·취소·추가 확정 필요·재확인·보관함), `/step/4` 화면 | `uv run pytest` 280개 통과, 브라우저에서 대안 A 선택 시에만 실행 조건 표시·저장 후 유지·R03에 공유 입력 채워짐·모든 질문 답하면 5단계 버튼 표시·보류는 추가 확정 필요로 기록·400px 정상·콘솔 오류 없음 |
| 9/16 | 규칙 ID·코드 함수 일치 검사를 요청 처리 중에서 앱 로드 시점으로 옮김 | 규칙 JSON에 R08을 임시로 넣고 `import policy_signal_map.app` 실행 → 종료 코드 1, 문구 "실행 함수가 없는 규칙: R08" 확인 후 파일 복구(`git diff` 변경 없음), `uv run pytest` 238개 통과 |
| 9/16 | 3번 자체 검토 후 수정: 질문 식별자를 규칙 ID로 바꿔 4단계 선택이 서로 덮어쓰지 않게 함(merge_group은 추가 입력 공유용으로 분리), 원안 변경 안내가 계속 남지 않도록 제출마다 다시 판단, 3단계 카드의 대안 미리보기 제거(대안은 4단계에서만), 규칙 JSON 전체 문구의 판정 단어 검사 추가 | `uv run pytest` 236개 통과, 질문 키 중복 없음·같은 내용 재제출 시 안내 사라짐을 테스트로 고정 |
| 9/16 | 3. 검토 질문 완료: `docs/review_rules.md` 초안(데이터 담당 검수 대기), 규칙 원본 JSON v1.0(문구·대안·merge_group), 규칙 실행(R07 판단 7단계, R01·R03·R04·R05, R06·R02는 검토하지 않음), 같은 자료를 묻는 질문은 카드를 합치지 않고 서로 표시, `/step/3` 화면(질문 카드·확인했으나 해당 없음·검토하지 않음 구분) | `uv run pytest` 232개 통과(규칙 30 + 화면 9 추가), 브라우저에서 예시+관광객 입력 시 질문 2건·추가 확정 필요 1건·해당 없음 2건·검토하지 않음 2건 확인, 400px 폭 정상, 콘솔 오류 없음 |
| 9/16 | AI 참고 의견(로컬 LLM) 구현: 규칙별 수치 없는 상황 설명(`llm_context`), 제공자 계층·로컬 호출(표준 라이브러리), 요청 문장, 출력 검사 6종, `/step/3/opinions`와 세션 캐시 | `uv run pytest` 388개 통과. 가짜 OpenAI 호환 서버로 끝까지 확인: 3단계 화면 9ms·의견 요청 별도, 수량 표현이 섞인 줄은 검사에서 버려짐(3줄 중 2줄만 표시), 모델·생성 시각 표시, 서버를 끄면 "AI 의견을 불러오지 못했습니다"만 표시되고 질문 카드는 정상, 콘솔 오류 없음. **실제 모델 품질·속도 확인(C-0)은 Ollama 설치 후** |
| 9/16 | 5-6 PDF로 저장 추가: 브라우저 인쇄 방식(`print.js`, `@media print`), 세 결과 화면에 [PDF로 저장] 버튼. 기존 결정 "인쇄/PDF 제외"를 변경 (`.md`는 고쳐 쓰는 원고, PDF는 공유용 사본) | `uv run pytest` 340개 통과, 인쇄 규칙을 화면에 적용해 A4 폭(186mm)에서 확인: 상단바·버튼 없음, 표 654px로 잘림 없음, 변경 줄 왼쪽 선 표시, 하단 고지 유지. **사용자 검토 완료** |
| 9/16 | 5. 보완 기획안과 저장 완료: 원안 문장 변환(`document/describe.py`), 선택 반영·변경 기록·8장·근거·요청서 초안(`builder.py`), Markdown 양식 2종, 저장 파일 이름, `/step/5` 비교 화면·전체 문서·요청서 초안·내려받기, 저장 위치 선택(`save.js`)과 근거 추적(`trace.js`), 임시 화면 삭제 | `uv run pytest` 338개 통과, 브라우저에서 예시 입력→대안 A 채택→5단계에서 변경 5건·추가 확정 필요 3건·근거 추적 패널·문서 본문 확인, 콘솔 오류 없음. 화면에 Markdown 기호(`**`)가 보이던 문제와 문서에 내부 키(`participation_data`)가 실리던 문제를 발견해 수정 |
| 9/16 | 2. 근거 확인 화면 완료 (계획 검토 13건 반영): 표시 형식(`formatting.py`, 사사오입·0.01 미만), 근거 상태 보관·오류 화면(전체 경로 비노출)·실제 자료+클라우드 시작 차단, 공통 렌더링, 화면 데이터 조립(요약 문장 3종, blocked 수치 숨김, 보류 사례는 합성만), `/step/2` 화면(차트 SRI·대체 문구, 월별·구간 표), 상단 자료 칩·하단 고지, 테스트 격리(`conftest.py`) | `uv run pytest` 193개 통과(셸에 PSM_ 변수 설정해도 동일), 공개 자료 검사 위반 0, 실제 자료+클라우드로 `uv run policy-signal-map` 실행 시 종료 코드 1, 브라우저: 계획서 9장 값과 화면 일치·차트 2개·분모 전환·보류 사례·CDN 주소를 임시로 틀리게 해 대체 문구 확인 후 복구·콘솔 오류 없음. 400px 폭에서 본문이 766px로 넓어지는 문제를 발견해 수정 후 381px 확인 |
| 9/17 | AI 참고 의견 실제 모델 확인(C-0)과 담당자 모델 선택(C-8): Ollama 0.34.1 + `exaone3.5:7.8b`(4·8비트)·`gemma4:26b-a4b-it-qat` 측정, Gemma는 생각 과정 출력을 꺼야 본문이 나와 `reasoning_effort "none"` 전송, 시간 한도 20→60초, `PSM_LLM_MODELS`·모델 표시 문구 파일·받아 둔 모델 확인·모델별 의견 보관·3단계 선택 칸 | `uv run pytest` 413개 통과. 합성 자료 + 실제 Ollama로 브라우저 확인: 모델 전환(첫 응답 12~16초), 돌아오면 보관한 의견 즉시, 목록 밖 모델 422, 콘솔 오류 없음, 400px 폭 넘침 없음. 측정표는 `6LLM참고의견계획.md` "C-0 측정 결과" |
| 9/17 | 실제 자료 식별 보강(6-0)과 해석 한계 구분 표시(6-0b): 임시 근거 파일 점검에서 `data_kind: actual_internal` 파일이 검증에 실패하면 실제 자료로 알아보지 못하는 것을 발견 → synthetic이 아닌 값은 모두 실제 자료로 보고, 검증에 실패해도 원문에서 확인(공개 자료 검사도 같은 기준). 2단계 해석 한계를 "자료의 한계(분석 담당 작성)"와 "서비스 해석 원칙"으로 나눠 표시 | `uv run pytest` 418개 통과, `check_public_bundle.py` 위반 없음. 임시 근거 파일이 실제 자료로 인식됨을 참/거짓만 출력해 확인(값은 보지 않음). 브라우저: 2단계 두 묶음 표시, 콘솔 오류 없음, 400px 폭 넘침 없음 |
| 9/17 | 6-5 전체 검토 수정: 원안 변경 후 재확인이 풀리지 않던 문제, 4단계를 건너뛰면 재확인 없이 문서가 만들어지던 문제, 쓰지 않는 실행 조건이 8장에 남던 문제(질문 사라짐·원안 유지로 변경·A→B), AI 의견 대기 중 원안 변경 시 잘못 보관, 결과 저장이 실패 응답을 파일로 내려받을 수 있던 문제. AI 응답 시간 한도 120초(사용자 결정) | `uv run pytest` 434개 통과(재현 테스트 16개 추가, 경합 테스트는 수정 전 실패 확인). 계획은 별도 작업 사본에 시제품으로 적용해 검증 후 반영. 브라우저(합성 자료): 5단계를 연 채 원안 변경 → [결과 저장] 누르면 재확인 안내(저장 창 안 뜸) → 상단 5단계 누르면 4단계로 이동·재확인 표시 → 저장 → 재확인 풀림·[보완 기획안 만들기] 표시 → 5단계 저장(저장 창 흉내)으로 문서 기록, 선택 취소 클릭 후 5단계가 4단계로 되돌림, 콘솔 오류 없음 |
| 9/17 | 6-5e 4단계 보관 안내를 이번 원안 변경분만 표시(원안을 여러 번 바꾸면 같은 질문이 쌓이고 다시 답한 질문도 계속 보관됐다고 나오던 문제). 실제 자료 + 로컬 LLM 허용 결정 기록 | `uv run pytest` 통과, 재현 시나리오(관광객 넣고 빼기 반복·사업명만 변경)를 테스트로 고정 |
| 9/17 | 6-4 브라우저 전체 흐름 확인(합성 자료, 창 없는 Chrome 1440px·400px, 콘솔 기록 수집). 확인 중 favicon 404 콘솔 기록과 4단계 "질문(없음)" 안내를 발견해 6-4a로 수정 | **① 정상 → 대안 A → 저장:** 예시 채움 → 2단계 차트 2개 생성·상단 "시연용 합성 수치" → 3단계 R07 질문·R05 추가 확정 필요·해당 없음·검토하지 않음 → 4단계 화면 입력칸으로 R07 채택·대안 A(수집 자료·협의 중·담당·주기) 저장 → [보완 기획안 만들기] → 5단계 "변경 5건 · 추가 확정 필요 3건 · 원안 유지 7개 장" → 전체 문서 → 내려받기 200, 파일명 `보완기획안_하반기_외국인_소비지원_쿠폰_20260917.md`, 콘솔 오류 0. **② 자료 부족 보류:** 2단계 보류 사례에 자료 없음·분모 0 표시 / R07 적용 상태를 needs_review로 바꾼 임시 사본(저장소 밖)으로 2단계 보류 문구·요약 숨김 → 3단계 R07 "보류" 카드 → 4단계 R07 선택 없이 [보완 기획안 만들기] → 5단계 문서 8장에 R07 보류 기록, 콘솔 오류 0. **③ 선택 후 취소, ④ 5단계 → 원안 변경 → 재확인:** 같은 날 6-5 확인(위 행). **⑤ 400px:** 1~5단계·전체 문서·요청서 초안 7개 화면 모두 문서 폭 400 = 화면 폭 400, 콘솔 오류 0. 6-4a: 관광객 기획에서 R07·R03 카드에 "같은 자료를 묻는 질문(R03)/(R07)과 함께 쓰는 입력" 표시, 예시 기획에서는 생략 |
| 9/17 | 8-2 사용법과 화면 캡처: 캡처 스크립트(`scripts/capture_screenshots.py`)로 합성 화면 6장, `docs/usage_guide.md` 작성. 캡처 환경은 로컬 실행 + 합성 자료(사용자 결정) | 캡처 6장을 직접 열어 확인(차트 정상, 04 카드 머리 보임, 상단 "시연용 합성 수치", 주소창 없음), 03 AI 문장 3줄 사용자 확인, 캡처 중 콘솔 오류 0. 사용법의 버튼·안내 문구는 템플릿과 대조, 판정 표현 검사(화면 문구 인용 1곳 예외). `05_saved_markdown.png`(외부 편집기 화면)는 사용자 캡처 대기 |
| 9/17 | 폴더 README 30개 최신화·쉬운 설명(R-1~R-5): 모든 README를 "이 폴더는 무엇인가 / 파일 목록(쉬운 말) / 자세한 설명(개발자용)" 틀로 통일, `resources/prompts/`·`resources/llm/` README 새로 작성. 코드와 어긋난 내용 정정(대기 시간 120초·모델 목록 설정, 실제 자료 + 로컬 LLM 허용, document 폴더 구조·함수, 테스트 개수 437, 경계 사례 생성 구분, CSS 구역 순서 등) | 폴더마다 추적 파일이 모두 파일 목록에 있는지·README 안 상대 링크가 열리는지 스크립트로 확인, 설명한 함수·필드·설정 기본값·테스트 개수를 코드와 `pytest --collect-only`로 대조, `uv run pytest` 437 통과, `check_public_bundle.py` 위반 없음 |
| 9/18 | 데이터 담당 검수 반영(A-7·A-6)과 이번 범위 확정(A-1): R04 "31일 미만은 서비스 운영 기준이며 통계 기준이 아님"을 `docs/review_rules.md`·`review/README.md`·`basic_checks.py` 주석에 명시, R01·R03·R04 검수 칸에 2026-09-17 기록(R07·R05는 대기로 표시), R02 "검토하지 않음" 이유에 "현재 자료는 외국인 전체 기준이라 관광객만 따로 볼 수 없습니다" 추가, 시도(제주) 근거·업종 비율 화면 계산·쿠폰 실적 합성을 향후 확장으로 확정(1장) | `uv run pytest` 437개 통과, `check_public_bundle.py` 추적 파일 198개 위반 없음. 판단 기준값(`SHORT_PERIOD_DAYS`)과 화면 문구는 바꾸지 않아 규칙 동작 변화 없음. 캡처 6장 재생성(합성 자료, 상단 "시연용 합성 수치" 확인) 후 `03_questions.png`에서 R02 새 문구가 잘림 없이 표시되는 것을 눈으로 확인, 캡처 중 콘솔 오류 0. AI 참고 의견 3줄 재생성(Gemma 4 26B) |
| 9/18 | 문서·주석 최신성 점검과 정리: `작업진행.md` 6-2(시도 추가)를 "하지 않음"으로 체크, 6-3 남은 칸에서 시도 자료 제외, 0-2 전달 방법 정리, 옛 폴더 경로(`1차공모/…`) 5곳을 현재 위치로, 현재 상태 표의 테스트 수 388→437·AI 항목 갱신, `checks.md` 1장 전달 방법 행 확정, `docs/README.md` 규칙 설명서 검수 상태, `docs/review_rules.md` 문장 빈칸 이름을 JSON과 통일(`{목표}`→`{goal_label}` 등 4곳), README 최신화 날짜 4곳 | 점검 스크립트로 README 30개 구조·링크(깨짐 0), 환경변수 7개(코드=`.env.example`=README), 화면 주소 15개(모두 문서에 있음), 파일별 테스트 수 29개 표와 합계 437 일치, 규칙 JSON 문구 ↔ 설명서 불일치 0(수정 후), CSS 구역 12개·1,219줄 확인. `uv run pytest` 437개 통과, `check_public_bundle.py` 위반 없음 |
| 9/18 | 검토 규칙 관리 번호를 화면·문서에서 제거: 카드 제목·1단계 예정 목록·3단계 오른쪽 목록·함께 확인 안내·4단계 보관/재확인 안내·AI 의견 문장과 근거 배지·5단계 변경 표·근거 추적·기획안 8장·별첨 표를 모두 규칙 제목으로 바꿈. `ReviewOutcome.related_titles`, `Change.rule_title` 추가, AI 의견은 `display_text`와 `rule_labels`로 전달 | `uv run pytest` 437개 통과(문구 검사 14곳 갱신), `check_public_bundle.py` 위반 없음. 캡처 6장 재생성 후 1·3·4·5단계를 눈으로 확인: 화면에 R01~R07 표기 없음(근거 파일 ID `DEMO-R07-NATIONAL`은 자료 이름이라 유지). 규칙 판단·질문 문구는 그대로 |

## 6. 남은 작업·막힌 결정

- [ ] 공개 배포 여부·호스팅 방식 결정 — **보류** (사용자 결정 9/16)
- [x] ~~제출 화면 캡처 방법 결정~~ → 9/17 로컬 실행 + 합성 자료로 결정(사용자), 캡처 6장 완료(`docs/screenshots/`). 외부 편집기 화면 1장은 사용자 캡처 대기
- [ ] 1장 미정 항목을 데이터 담당과 확정
- [ ] 2장 연결 확인 시간 확정
- [ ] 3장 전달 형식 공동 확인 — 9/17 데이터 담당 확정 기록. 확정본에서 `data_kind`·`month` 표기 반영 확인 남음
- [x] ~~A→C 확장과 LLM 전달 자료 원칙(①+②)을 데이터 담당과 합의하고, 합의 시 최종기획서 8장·11장 수정~~ → 9/16 로컬 LLM만 쓰기로 해 외부 전송이 없으므로 8장 수정 불필요. 남은 것은 아래 한 줄
- [ ] 최종기획서 11장 "조건부 확장"에 AI 참고 의견(로컬 LLM) 한 줄을 넣을지 결정
- [x] ~~실제 자료로 AI 참고 의견을 쓸지~~ → 9/17 로컬 LLM은 허용으로 결정(1장). 6-1 대조 중에는 끈다
- [ ] `docs/review_rules.md` 검수 (조건·문구·문서 문장·AI용 상황 설명) — **R01·R03·R04는 9/17 데이터 담당 검수 완료, 9/18 반영.** R07·R05 검수 대기(의견 오면 문구 수정 후 3·4단계 캡처 재생성)
- [ ] **추가로 확보할 자료** (데이터 담당 결정 문서 9/15 5장, 9/17 기록)
  - [ ] 참여 점포 식별정보와 카드 업종 연결표 — 9/21 전 확보 어려움(개발 판단). 대안 D 정밀 분석 요청서 초안의 확인 항목, 제출 PDF 향후 과제로 표시
  - [ ] 외국인 전체와 관광객을 구분하는 자료 — 제공 자료에 없음. R02 보류 유지 근거, R03 질문이 다루는 한계. **9/18: 이 한계를 R02 "검토하지 않음" 화면 문구에도 적음**
  - [ ] 운영 사례 원문 검수(출처·발표일·대상·사용처·모집·정산) — 제출 PDF "외부자료 활용" 근거 후보. 검수 전에는 확정 근거로 쓰지 않음. 담당·기한 미정
  - [ ] 원본 보고서 확보: `제공데이터_기반_기획안_재검토보고서.md`, 기존 유사성 검토보고서 — 작업 폴더에 없음. 8-1 대안 비교표와 제출 PDF 분석 예시 확인용
